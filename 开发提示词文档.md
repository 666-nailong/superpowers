# 电路分析学习助手 — 微信小程序开发提示词文档

> 本文档用于向 AI 编码助手完整描述项目需求和设计，作为生成代码的输入提示词。

---

## 一、项目概述

一个帮助大学本科生学习《电路分析》课程的微信小程序。支持课件学习（含公共批注互动）、题库练习、公式速查、AI 答疑、个人学习数据统计五大模块。

- **技术栈：** 微信原生小程序（WXML + WXSS + JavaScript）
- **存储方案：** 微信本地存储（`wx.setStorageSync` / `wx.getStorageSync`），无需后端服务器
- **设计风格：** 极简实用，大量留白，深蓝色主调
- **最低基础库：** 2.30.0（微信 8.0+）

---

## 二、全局配置（app.json）

### 2.1 页面路由

共 12 个页面，注册顺序如下：

```
pages/
├── pages/index/index                    # 首页（学习仪表盘）
├── pages/courseware/courseware          # 课件列表（按章节）
├── pages/courseware-viewer/viewer       # 课件阅读器（PDF浏览+批注）
├── pages/question-bank/bank             # 题库首页（章节/真题/模拟考入口）
├── pages/question-practice/practice     # 答题页面（练习模式/考试模式）
├── pages/question-result/result         # 答题结果页（得分+解析）
├── pages/wrong-book/wrong-book          # 错题本
├── pages/favorites/favorites            # 收藏夹（题目+公式）
├── pages/formulas/formulas              # 公式速查
├── pages/ai-assistant/assistant         # AI 答疑聊天页
├── pages/profile/profile                # 个人中心
├── pages/my-annotations/annotations     # 我的批注列表
```

### 2.2 底部 TabBar

| 图标 | 文字 | 页面路径 |
|------|------|---------|
| 📖 | 学习 | pages/index/index |
| 📝 | 题库 | pages/question-bank/bank |
| ∑ | 公式 | pages/formulas/formulas |
| 🤖 | AI | pages/ai-assistant/assistant |
| 👤 | 我的 | pages/profile/profile |

> 图标使用小程序默认 icon 或 纯文本/Unicode 符号，无需额外图标库。

### 2.3 全局样式变量（app.wxss）

```css
/* 主色调 */
--primary: #1A365D;
--primary-light: #2B4C7E;
--primary-dark: #0F2440;
--accent: #3B82F6;
--success: #10B981;
--warning: #F59E0B;
--danger: #EF4444;

/* 中性色 */
--bg: #F7F8FA;
--card-bg: #FFFFFF;
--text-primary: #1A202C;
--text-secondary: #4A5568;
--text-tertiary: #A0AEC0;
--border: #E2E8F0;

/* 尺寸 */
--radius-sm: 8rpx;
--radius-md: 12rpx;
--radius-lg: 16rpx;
--page-padding: 32rpx;
```

---

## 三、数据模型（本地存储）

所有数据存于 `wx.setStorageSync`，共 8 个存储集合：

### 3.1 学习课件数据 `courseware_data`

```javascript
// 存储键: 'courseware_data'
// 类型: Array<Chapter>

{
  chapters: [
    {
      id: "ch01",
      title: "电路模型和电路定律",
      order: 1,
      files: [
        {
          id: "ch01_01",
          title: "§1.1 电路和电路模型",
          // PDF 在项目中作为静态资源放在 assets/pdf/ 下
          pdfUrl: "assets/pdf/ch01_01.pdf",
          totalPages: 12
        }
      ]
    }
  ]
}
```

### 3.2 题库数据 `question_data`

```javascript
// 存储键: 'question_data'
// 类型: Object 包含 chapters, exams, mockExams

{
  chapters: [
    {
      id: "qch01",
      title: "第一章 电路模型和电路定律",
      questions: [
        {
          id: "q001",
          type: "choice",        // choice | fill | judge
          difficulty: 1,         // 1-5
          question: "理想电压源的特点是什么？",
          options: [
            "A. 输出电压恒定，电流由外电路决定",
            "B. 输出电流恒定，电压由外电路决定",
            "C. 输出电压和电流都恒定",
            "D. 输出电压和电流都由外电路决定"
          ],
          answer: 0,             // 索引（单选）或数组（多选）
          explanation: "理想电压源的内阻为0，输出电压恒定...",
          chapter: "qch01",
          tags: ["电压源", "理想元件"]
        }
      ]
    }
  ],
  pastExams: [
    {
      id: "exam2023",
      year: "2023",
      title: "2023年秋季学期期末考试",
      questions: ["q001", "q005", "q012", ...]  // 引用 question id
    }
  ],
  mockExamConfig: {
    questionCount: 20,
    timeLimit: 60,              // 分钟
    chapterRatio: {             // 各章节抽题比例
      qch01: 0.15,
      qch02: 0.20,
      qch03: 0.20,
      qch04: 0.20,
      qch05: 0.15,
      qch06: 0.10
    }
  }
}
```

### 3.3 答题记录 `answer_records`

```javascript
// 存储键: 'answer_records'
// 类型: Object 以 questionId 为 key

{
  "q001": {
    questionId: "q001",
    attempts: [
      {
        timestamp: 1703000000000,
        answer: 1,
        isCorrect: false,
        mode: "practice"       // practice | exam | mock
      }
    ],
    lastAttempt: 1703000000000,
    totalAttempts: 3,
    correctCount: 1,
    wrongCount: 2
  }
}
```

### 3.4 错题本 `wrong_book`

```javascript
// 存储键: 'wrong_book'
// 类型: Array<WrongItem>

[
  {
    questionId: "q001",
    wrongAnswer: 1,
    chapterId: "qch01",
    addedAt: 1703000000000,
    mastered: false            // 是否已掌握
  }
]
```

### 3.5 收藏夹 `favorites`

```javascript
// 存储键: 'favorites'
// 类型: Object

{
  questions: ["q001", "q005"],
  formulas: ["ohm-law", "kvl"]
}
```

### 3.6 课件批注 `courseware_annotations`

```javascript
// 存储键: 'courseware_annotations'
// 类型: Object 以 "fileId_pageNum" 为 key

{
  "ch01_01_3": {
    fileId: "ch01_01",
    pageNum: 3,
    annotations: [
      {
        id: "ann_001",
        userId: "default_user",
        userName: "匿名同学",
        avatar: "",              // 留空使用默认头像
        content: "这里KVL方程要注意参考方向！",
        createdAt: 1703000000000,
        likes: 5,
        likedBy: ["uid1", "uid2"],
        replies: [
          {
            id: "rep_001",
            userId: "other_user",
            userName: "学霸小明",
            content: "对，正负号很容易搞反",
            createdAt: 1703001000000,
            likes: 2
          }
        ]
      }
    ]
  }
}
```

### 3.7 用户批注索引 `my_annotations_index`

```javascript
// 存储键: 'my_annotations_index'
// 类型: Array 记录该用户发表过的所有批注

[
  {
    annotationId: "ann_001",
    fileId: "ch01_01",
    pageNum: 3,
    content: "这里KVL方程要注意参考方向！",
    createdAt: 1703000000000
  }
]
```

### 3.8 公式数据 `formula_data`

```javascript
// 存储键: 'formula_data'
// 类型: Object

{
  categories: [
    {
      id: "fcat_basic",
      title: "基本定律",
      formulas: [
        {
          id: "ohm-law",
          title: "欧姆定律",
          latex: "U = IR",
          description: "线性电阻两端电压与通过电流成正比",
          tags: ["欧姆定律", "电阻"]
        },
        {
          id: "kvl",
          title: "基尔霍夫电压定律(KVL)",
          latex: "\\sum_{k=1}^{n} U_k = 0",
          description: "任一回路中，各元件电压的代数和为零",
          tags: ["KVL", "基尔霍夫"]
        },
        {
          id: "kcl",
          title: "基尔霍夫电流定律(KCL)",
          latex: "\\sum_{k=1}^{n} I_k = 0",
          description: "任一节点上，电流的代数和为零",
          tags: ["KCL", "基尔霍夫"]
        }
      ]
    },
    {
      id: "fcat_ac",
      title: "交流电路",
      formulas: [
        {
          id: "impedance",
          title: "阻抗",
          latex: "Z = R + jX = |Z|\\angle\\varphi",
          description: "阻抗 = 电阻 + 电抗",
          tags: ["阻抗", "交流"]
        }
      ]
    },
    {
      id: "fcat_transient",
      title: "暂态分析",
      formulas: [
        {
          id: "rc-transient",
          title: "RC电路零输入响应",
          latex: "u_C(t) = U_0 e^{-\\frac{t}{RC}}",
          description: "电容放电电压随时间指数衰减",
          tags: ["暂态", "RC", "零输入"]
        }
      ]
    }
  ]
}
```

### 3.9 AI 对话历史 `ai_chat_history`

```javascript
// 存储键: 'ai_chat_history'
// 类型: Array<Message>

[
  {
    id: "msg_001",
    role: "user",              // user | assistant
    content: "解释一下叠加定理",
    timestamp: 1703000000000,
    shared: false
  }
]
```

### 3.10 学习统计 `learning_stats`

```javascript
// 存储键: 'learning_stats'
// 类型: Object

{
  totalStudyTime: 3600,               // 秒（累计学习时长）
  chaptersCompleted: [                 // 已完成的课件章节
    "ch01", "ch02"
  ],
  totalQuestionsAnswered: 150,
  totalCorrect: 120,
  totalWrong: 30,
  mockExamHistory: [                  // 模拟考试记录
    {
      date: 1703000000000,
      score: 85,
      totalQuestions: 20,
      correctCount: 17,
      timeUsed: 45                    // 分钟
    }
  ],
  // 连续打卡天数（以答题/学习为一天）
  streakDays: 5,
  lastStudyDate: "2024-12-20"
}
```

---

## 四、详细页面设计

### 4.1 首页（pages/index/index）

**功能：** 学习仪表盘，展示学习概览和快捷入口。

**页面布局：**

```
┌─────────────────────────────┐
│  顶部: 用户头像 + 昵称      │
│        "今天也要加油！"      │
├─────────────────────────────┤
│  学习进度卡片                │
│  ┌───┬───┬───┬───┬───┐    │
│  │已学│正确│错题│连续│时长│    │
│  │章节│率  │   │天数│    │    │
│  └───┴───┴───┴───┴───┘    │
├─────────────────────────────┤
│  快捷入口（2×2 网格）        │
│  ┌────────┬────────┐       │
│  │ 📖     │ 📝     │       │
│  │ 继续学习│ 刷题   │       │
│  ├────────┼────────┤       │
│  │ 📕     │ 📊     │       │
│  │ 错题重做│ 模拟考试│       │
│  └────────┴────────┘       │
├─────────────────────────────┤
│  最近学习记录（3-5条）       │
│  · 第3章 电阻电路的一般分析   │
│  · 做对了12/15题             │
│  · ...                       │
└─────────────────────────────┘
```

**数据来源：** `learning_stats` + `answer_records`

**交互：**
- 点击"继续学习" → 跳转到上次学习的课件页面（存 `lastCoursewareId` 在 `learning_stats`）
- 统计数字点击 → 跳转到对应页面
- 继续学习和刷题按实际数据动态读取

---

### 4.2 课件列表（pages/courseware/courseware）

**功能：** 按教材章节展示课件目录。

**页面布局：**

```
┌─────────────────────────────┐
│  返回  课件列表             │
├─────────────────────────────┤
│  ▸ 第一章 电路模型和电路定律 │
│    ├ §1.1 电路和电路模型    │
│    ├ §1.2 电流和电压        │
│    └ §1.3 电功率和能量      │
│  ▸ 第二章 电阻电路的等效变换│
│  ▸ 第三章 电阻电路的一般分析│
│  ...                        │
└─────────────────────────────┘
```

**交互：**
- 章节标题可折叠展开（点击 ▸ 展开/收起）
- 点击小节 → 跳转到 `courseware-viewer`，传入 `fileId`
- 每个小节右侧显示该页批注总数（跨页汇总）

---

### 4.3 课件阅读器（pages/courseware-viewer/viewer）

**功能：** 核心页面。浏览 PDF 课件内容、添加/查看批注、AI 提问。

**这是整个小程序最复杂的页面。**

```
┌─────────────────────────────┐
│  返回   §1.1 电路和电路模型  │ [页码] │
├─────────────────────────────┤
│                             │
│   [课件内容区域]             │
│   由于微信小程序不支持直接   │
│   渲染 PDF，采用每页截取为   │
│   长图/按页切图的方式展示    │
│                             │
│   图片使用 <image> 组件      │
│   支持双指缩放+拖动          │
│                             │
│   ┌─ 页面批注标记 ──┐       │
│   │  [03]  ← 点击展开批注  │
│   └─────────────────┘       │
│                             │
├─────────────────────────────┤
│  ◀ 上一页      下一页 ▶    │
│              [AI提问] (悬浮) │
└─────────────────────────────┘
```

**交互细节：**

**（A）课件内容展示：**
- 每页课件用 `<image>` 组件加载预切好的页面图片
- 图片路径规则：`assets/pdf_slices/{fileId}/page_{num}.jpg`
- 支持双指缩放（`<scroll-view>` 包裹或其他手势方案）
- 底部有翻页导航（页码显示 + 左右箭头）
- 滑动翻页：使用 `swiper` 组件实现左右滑动切换页面

**（B）批注功能：**
- 每页右上角/左上角显示批注数量标记（如 `03` 表示有 3 条批注）
- 无批注时不显示标记
- **点击数字标记** → 从底部弹出批注面板（`<popup>` 从底部划入）
- 批注面板内容：
  - 按点赞数降序排列的批注列表
  - 每条批注显示：用户名 + 内容 + 时间 + 点赞数 + 回复数
  - 点赞按钮（❤/🤍 切换）
  - 回复按钮 → 展开回复列表 + 输入框
- 页面底部有浮动"添加批注"输入框（或长按页面触发添加）

**（C）添加批注流程：**
1. 点击页面底部"添加批注"按钮（或悬浮 + 按钮）
2. 弹出输入框（带字符计数，限 200 字）
3. 提交 → 写入 `courseware_annotations` + `my_annotations_index`
4. 提交后该页批注标记数字 +1

**（D）AI 提问悬浮按钮：**
- 右下角悬浮圆形按钮（深蓝色），带 🤖 或 ? 图标
- 点击 → 跳转至 `ai-assistant` 页面，自动附带上下文
  ```
  // 传入的上下文格式
  {
    from: "courseware",
    fileId: "ch01_01",
    pageNum: 3,
    chapterTitle: "电路和电路模型",
    pageContent: "..."  // 可选，当前页面文本（如果有OCR提取）
  }
  ```

---

### 4.4 题库首页（pages/question-bank/bank）

**功能：** 三个入口：章节练习、真题专区、模拟考试。

```
┌─────────────────────────────┐
│  返回  题库                 │
├─────────────────────────────┤
│  ┌──────────────────────┐  │
│  │ 📝 章节练习           │  │
│  │ 按教材章节分类刷题     │  │
│  │ ▸ 第一章 (12/20 已做)  │  │
│  │ ▸ 第二章 (0/18 未做)   │  │
│  │ ▸ 第三章 (5/15 已做)   │  │
│  └──────────────────────┘  │
│                             │
│  ┌──────────────────────┐  │
│  │ 📋 真题专区           │  │
│  │ 历年考试真题          │  │
│  │ ▸ 2023年期末考试       │  │
│  │ ▸ 2022年期末考试       │  │
│  │ ▸ 2021年期末考试       │  │
│  └──────────────────────┘  │
│                             │
│  ┌──────────────────────┐  │
│  │ 📊 模拟考试           │  │
│  │ 随机组卷，限时答题     │  │
│  │ [开始模拟考试]         │  │
│  │ 上次: 85分 (12月20日)  │  │
│  └──────────────────────┘  │
└─────────────────────────────┘
```

---

### 4.5 答题页面（pages/question-practice/practice）

**功能：** 核心答题界面。支持练习/考试两种模式。

```
┌─────────────────────────────┐
│  返回  第一章  第3/20题     │ 计时器（考试模式）
├─────────────────────────────┤
│  题目区域                    │
│  ┌──────────────────────┐  │
│  │ [单选题] ★★★☆☆       │  │
│  │                      │  │
│  │ 理想电压源的特点是     │  │
│  │ 什么？                │  │
│  └──────────────────────┘  │
│                             │
│  选项区域                    │
│  ┌──────────────────────┐  │
│  │ ○ A. 输出电压恒定     │  │
│  ├──────────────────────┤  │
│  │ ○ B. 输出电流恒定     │  │
│  ├──────────────────────┤  │
│  │ ○ C. 电压电流都恒定   │  │
│  ├──────────────────────┤  │
│  │ ○ D. 电压电流由...   │  │
│  └──────────────────────┘  │
│                             │
│  ┌────────┐  ┌────────┐   │
│  │ 上一题  │  │ 下一题  │   │
│  └────────┘  └────────┘   │
│                             │
│  底部: 答题卡 + 交卷按钮    │
│  [1][2][3][4][5]...[20]    │
│         [交卷]              │
└─────────────────────────────┘
```

**交互细节：**

**练习模式（做完一题显示一题解析）：**
- 选择答案后立即显示正确/错误（绿色/红色）
- 下方展开解析区域
- 自动进入下一题或手动点击"下一题"

**考试模式（全部做完统一批改）：**
- 不显示正误，可随时切换答案
- 顶部显示倒计时
- 底部答题卡标记：未答=灰色，已答=蓝色，当前=深蓝边框
- 点击交卷 → 跳转到结果页

**答题卡交互：**
- 点击数字跳转到对应题目
- 点击"交卷"确认弹窗

---

### 4.6 答题结果页（pages/question-result/result）

**功能：** 考试模式交卷后展示成绩和错题解析。

```
┌─────────────────────────────┐
│  答题结果                    │
├─────────────────────────────┤
│  ┌──────────────────────┐  │
│  │      得分: 85分        │  │
│  │     ⭐⭐⭐⭐☆           │  │
│  │   正确 17/20 题        │  │
│  │   用时 45分钟          │  │
│  └──────────────────────┘  │
│                             │
│  错题解析列表                │
│  ┌──────────────────────┐  │
│  │ ❌ 第3题 你做错了      │  │
│  │ ...                   │  │
│  │ 正确答案: A            │  │
│  │ 解析: ...              │  │
│  │ [加入错题本] [收藏]    │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ ❌ 第8题 ...          │  │
│  └──────────────────────┘  │
│                             │
│  [返回首页]  [重新答题]    │
└─────────────────────────────┘
```

---

### 4.7 错题本（pages/wrong-book/wrong-book）

```
┌─────────────────────────────┐
│  返回  错题本  [章节筛选▼]  │
├─────────────────────────────┤
│  错题统计: 共23题，已掌握8题 │
│                             │
│  ┌──────────────────────┐  │
│  │ [第1题] ★★★☆☆        │  │
│  │ 题目内容...            │  │
│  │ 上次答错: 12月20日      │  │
│  │ [重做] [移除]          │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ [第2题] ★★☆☆☆        │  │
│  │ ...                   │  │
│  └──────────────────────┘  │
│                             │
│  [全部重做]                 │
└─────────────────────────────┘
```

**交互：**
- 顶部下拉筛选章节
- "掌握状态"切换：全部/未掌握/已掌握
- 点击题目展开详情（显示解析）
- 点击"重做" → 跳转到答题页面，仅含筛选出的错题
- 点击"全部重做" → 跳转到答题页面

---

### 4.8 收藏夹（pages/favorites/favorites）

```
┌─────────────────────────────┐
│  返回  我的收藏              │
├─────────────────────────────┤
│  [题目]  [公式]  ← Tab切换  │
├─────────────────────────────┤
│  (题目 tab)                  │
│  ┌──────────────────────┐  │
│  │ [第1题] ★★★☆☆        │  │
│  │ 题目内容...            │  │
│  │ 收藏于: 12月20日       │  │
│  │ [取消收藏] [做题]      │  │
│  └──────────────────────┘  │
│                             │
│  (公式 tab)                  │
│  ┌──────────────────────┐  │
│  │ 欧姆定律  U=IR        │  │
│  │ [取消收藏]            │  │
│  └──────────────────────┘  │
└─────────────────────────────┘
```

---

### 4.9 公式速查（pages/formulas/formulas）

```
┌─────────────────────────────┐
│  ∑ 公式速查   [🔍搜索公式]  │
├─────────────────────────────┤
│  ▸ 基本定律                 │
│  ┌──────────────────────┐  │
│  │ U = IR               │  │
│  │ 欧姆定律              │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ ΣU_k = 0             │  │
│  │ KVL基尔霍夫电压定律    │  │
│  └──────────────────────┘  │
│                             │
│  ▸ 交流电路                 │
│  ▸ 暂态分析                 │
│  ▸ 三相电路                 │
│  ▸ 二端口网络               │
└─────────────────────────────┘
```

**交互：**
- 公式卡片左滑 → 出现收藏按钮
- 点击公式 → 展开详情（大字体渲染 + 文字说明）
- 搜索框支持按公式名称/标签搜索
- 公式用 LaTeX 语法存储在数据中，渲染方案：
  - 方案一：使用 `wx小程序` 的 `rich-text` + 预渲染图片
  - 方案二：使用 **简化方案** — 直接使用 Unicode 数学符号 + HTML 排版（上标/下标），以文本方式展示
  - 推荐方案二，性能好且无需额外依赖

对于公式渲染，使用纯文本排版方案：
- 分式：`U = IR` `X_L = ωL`
- 下标：用 `<text style="vertical-align: sub">` 或 Unicode 下标字符
- 积分/求和：用 `∑` `∫` 字符 + 文本排版
- 根号：用 `√` 字符
- 分数线：可用 `───` 辅助线或直接横线

具体示例：
- `U_C(t) = U₀e^(-t/RC)` 用 Unicode 下标₀
- `Z = R + jX = |Z|∠φ`
- `∑U_k = 0` (KVL)

---

### 4.10 AI 答疑（pages/ai-assistant/assistant）

**功能：** 与 AI 对话，解答电路分析问题。

```
┌─────────────────────────────┐
│  ← 返回   AI答疑   [分享]   │
├─────────────────────────────┤
│                             │
│  [电路分析AI助手]            │
│  你好！我是电路分析学习助手，  │
│  有什么问题可以问我～         │
│                             │
│  ┌──────────────────────┐  │
│  │ 请解释一下叠加定理     │  │ → 用户消息（右对齐）
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ 叠加定理：在线性电路中  │  │ ← AI回复（左对齐，深蓝底白字）
│  │ ...                   │  │
│  └──────────────────────┘  │
│                             │
│  ┌─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐   │
│  │ 叠加定理的内容是...     │  │ 历史对话
│  └─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘   │
│                             │
├─────────────────────────────┤
│  [📎] [输入问题...]  [发送] │
└─────────────────────────────┘
```

**交互：**
- 用户消息右对齐（浅灰气泡）
- AI 回复左对齐（深蓝气泡，白色文字）
- 长按 AI 回复 → 弹出"分享"选项（生成分享图或复制链接）
- 顶部"分享"按钮 → 分享当前对话给好友
- 从课件页跳转过来时，自动发送一条上下文消息
  ```
  "我在学习 §1.1 电路和电路模型 第3页，能帮我讲解一下这部分吗？"
  ```
- 因为无后端，AI 回答使用**本地模拟回复**（预置常见问题的回答库），或者调用第三方 API（如 讯飞星火/DeepSeek）作为可选项

**预置问答库示例：**
```javascript
const presetAnswers = [
  {
    keywords: ["叠加定理", "superposition"],
    answer: "叠加定理：在线性电路中，任一电压或电流都是各个独立电源单独作用时，在该处产生的电压或电流的代数和。\n\n使用步骤：\n1. 每次只保留一个独立源，其他电压源短路、电流源开路\n2. 分别计算各电源单独作用时的响应\n3. 将各响应代数相加\n\n⚠️ 注意：叠加定理只适用于电压和电流，不适用于功率！"
  },
  {
    keywords: ["KVL", "基尔霍夫电压"],
    answer: "KVL（基尔霍夫电压定律）：\n在任一回路中，各元件电压的代数和恒等于零。\n\n公式：∑U = 0\n\n注意：\n• 需要先选定回路绕行方向\n• 电压参考方向与绕行方向一致取正，相反取负\n• 适用于任何集中参数电路"
  }
]
```

---

### 4.11 个人中心（pages/profile/profile）

```
┌─────────────────────────────┐
│  个人中心                    │
├─────────────────────────────┤
│  用户头像 + 昵称             │
│  (暂用默认头像 + "电路学习者")│
├─────────────────────────────┤
│  学习数据                    │
│  ┌──────────────────────┐  │
│  │ 📊 学习统计            │  │
│  │ 已完成章节: 2/6        │  │
│  │ 已做题目: 150          │  │
│  │ 正确率: 80%            │  │
│  │ 累计时长: 3.5h         │  │
│  │ 连续打卡: 5天           │  │
│  └──────────────────────┘  │
├─────────────────────────────┤
│  功能入口                    │
│  📕 错题本 (23)             │
│  ⭐ 我的收藏                 │
│  ✏️ 我的批注                 │
├─────────────────────────────┤
│  设置                       │
│  🗑 清除缓存                │
│  ℹ️ 关于我们                 │
└─────────────────────────────┘
```

### 4.12 我的批注列表（pages/my-annotations/annotations）

```
┌─────────────────────────────┐
│  返回  我的批注 (共12条)     │
├─────────────────────────────┤
│  ┌──────────────────────┐  │
│  │ "这里KVL方程要注意..." │  │
│  │ §1.1 第3页 · 12月20日  │  │
│  │   ♥ 5人点赞  💬 2回复  │  │
│  │  [查看上下文] [删除]    │  │
│  └──────────────────────┘  │
│  ┌──────────────────────┐  │
│  │ "相位差的计算..."     │  │
│  │ §3.2 第8页 · 12月19日  │  │
│  │   ♥ 3人点赞  💬 0回复  │  │
│  └──────────────────────┘  │
└─────────────────────────────┘
```

**交互：**
- 点击"查看上下文" → 跳转到 `courseware-viewer`，定位到对应文件+页面
- 点击"删除" → 确认弹窗 → 删除该批注及所有关联数据
- 按时间倒序排列

---

## 五、组件设计

### 5.1 组件清单

| 组件名 | 用途 | 所在页面 |
|--------|------|---------|
| `stats-card` | 展示数字统计（已学章节/正确率等） | 首页 |
| `chapter-list` | 可折叠章节列表 | 课件列表、题库 |
| `annotation-badge` | 批注数量标记气泡 | 课件阅读器 |
| `annotation-panel` | 底部弹出批注面板 | 课件阅读器 |
| `question-card` | 题目展示卡片（含选项） | 答题页面 |
| `answer-card` | 答题卡网格（跳转导航） | 答题页面 |
| `result-overview` | 考试结果概览卡片 | 答题结果页 |
| `formula-card` | 公式展示卡片 | 公式速查 |
| `chat-message` | 聊天消息气泡 | AI 答疑 |
| `loading` | 加载状态组件 | 全局 |
| `empty-state` | 空状态提示 | 全局 |

---

## 六、数据存储与初始化

### 6.1 首次启动

在 `app.js` 的 `onLaunch` 中检查是否为首次启动：

```javascript
onLaunch() {
  const inited = wx.getStorageSync('app_inited')
  if (!inited) {
    this.initDefaultData()
    wx.setStorageSync('app_inited', true)
  }
}

initDefaultData() {
  // 写入示例课件数据
  wx.setStorageSync('courseware_data', defaultCoursewareData)
  // 写入示例题库数据
  wx.setStorageSync('question_data', defaultQuestionData)
  // 写入示例公式数据
  wx.setStorageSync('formula_data', defaultFormulaData)
  // 初始化空的用户数据
  wx.setStorageSync('answer_records', {})
  wx.setStorageSync('wrong_book', [])
  wx.setStorageSync('favorites', { questions: [], formulas: [] })
  wx.setStorageSync('courseware_annotations', {})
  wx.setStorageSync('my_annotations_index', [])
  wx.setStorageSync('ai_chat_history', [])
  wx.setStorageSync('learning_stats', {
    totalStudyTime: 0,
    chaptersCompleted: [],
    totalQuestionsAnswered: 0,
    totalCorrect: 0,
    totalWrong: 0,
    mockExamHistory: [],
    streakDays: 0,
    lastStudyDate: '',
    lastCoursewareId: null
  })
}
```

### 6.2 存储工具函数（utils/storage.js）

封装统一的 CRUD 工具函数：

```javascript
// 通用
export const getData = (key) => wx.getStorageSync(key) || null
export const setData = (key, data) => wx.setStorageSync(key, data)

// 错题本专用
export const addWrongQuestion = (questionId, wrongAnswer, chapterId) => { ... }
export const getWrongQuestions = (chapterId) => { ... }
export const markAsMastered = (questionId) => { ... }
export const removeWrongQuestion = (questionId) => { ... }

// 答题记录专用
export const recordAnswer = (questionId, answer, isCorrect, mode) => { ... }
export const getQuestionStats = (questionId) => { ... }

// 批注专用
export const addAnnotation = (fileId, pageNum, content) => { ... }
export const getPageAnnotations = (fileId, pageNum) => { ... }
export const likeAnnotation = (annotationId, fileId, pageNum) => { ... }
export const replyAnnotation = (annotationId, fileId, pageNum, content) => { ... }
export const deleteAnnotation = (annotationId, fileId, pageNum) => { ... }

// 收藏专用
export const toggleFavoriteQuestion = (questionId) => { ... }
export const toggleFavoriteFormula = (formulaId) => { ... }

// 学习统计
export const updateStudyTime = (seconds) => { ... }
export const updateStatsAfterAnswer = (isCorrect) => { ... }
export const updateStreak = () => { ... }
```

---

## 七、样式规范

### 7.1 设计原则

- **极简实用**：无多余装饰，文字和功能直接呈现
- **大量留白**：卡片间至少 24rpx 间距，内容区至少 32rpx 内边距
- **线条简洁**：使用 `1px` 细边框，避免阴影和渐变
- **深蓝主调**：`#1A365D`，辅以 `#3B82F6` 作为交互色

### 7.2 组件样式要求

| 元素 | 样式 |
|------|------|
| 卡片 | 白底，圆角 `12rpx`，无阴影（仅用 `1rpx solid #E2E8F0` 边框） |
| 按钮（主） | 深蓝底 `#1A365D` 白字，圆角 `8rpx`，高 `80rpx` |
| 按钮（次） | 白底 `#1A365D` 边框/文字，圆角 `8rpx`，高 `72rpx` |
| 输入框 | 浅灰底 `#F7F8FA`，无边框，圆角 `8rpx`，高 `72rpx` |
| Tab | 下划线式，选中时深蓝底白色文字标签 |
| 标题 | 字号 `32rpx`，字重 `600`，色 `#1A202C` |
| 正文 | 字号 `28rpx`，字重 `400`，色 `#4A5568` |
| 辅助文字 | 字号 `24rpx`，色 `#A0AEC0` |

### 7.3 响应式

- 设计基准：iPhone 6/7/8 宽度（375px / 750rpx）
- 字体用 `rpx` 单位自动缩放
- 不使用 `px` 硬编码尺寸

---

## 八、页面间传参

| 来源 | 目标 | 传参 |
|------|------|------|
| 课件列表 | 课件阅读器 | `fileId`, `fileTitle` |
| 课件阅读器 | AI答疑 | `from`, `fileId`, `pageNum`, `chapterTitle` |
| 题库首页 | 答题页面 | `mode`, `chapterId`, `questionIds`（错题时传入 ID 数组） |
| 错题本 | 答题页面 | `mode: "wrong"`, `questionIds: [...]` |
| 我的批注 | 课件阅读器 | `fileId`, `pageNum` |
| 首页 "继续学习" | 课件阅读器 | `fileId: lastCoursewareId` |
| 答题页面 | 结果页 | `score`, `total`, `correct`, `wrongIds`, `timeUsed`, `answers` |

---

## 九、开发顺序（建议）

| 阶段 | 内容 | 预估 |
|------|------|------|
| Phase 1 | 项目框架搭建 + TabBar + 全局样式 | 1天 |
| Phase 2 | 首页 + 个人中心 + 学习统计 | 2天 |
| Phase 3 | 公式速查（结构简单，快速完成） | 1天 |
| Phase 4 | 题库模块（题库首页 + 答题页 + 结果页 + 错题本 + 收藏夹） | 4天 |
| Phase 5 | 课件模块（课件列表 + 阅读器 + 批注系统） | 4天 |
| Phase 6 | AI 答疑（聊天界面 + 预置问答库） | 2天 |
| Phase 7 | 联调 + 数据初始化 + 边界情况处理 | 2天 |

---

## 十、交付标准

1. ✅ 全部页面可正常跳转
2. ✅ 本地数据可读写，首次启动自动初始化示例数据
3. ✅ 答题流程完整（选题→作答→判对错→收录错题→查看解析）
4. ✅ 课件批注可增删查，点赞和回复正常
5. ✅ 公式速查可搜索可收藏
6. ✅ AI 答疑可对话，预置问答库正常回复
7. ✅ 学习统计正确累计
8. ✅ 样式统一，深蓝色主题一致
9. ✅ 所有页面在 iPhone 和 Android 微信上渲染正常
